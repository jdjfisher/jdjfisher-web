---
interface Props {
  text: string;
  colour: string;
}

const { text, colour } = Astro.props;
---

<code
  class="rounded-full px-3 py-1 text-xs shadow-inner select-none"
  style={{ backgroundColor: colour }}
>
  {text}
</code>
